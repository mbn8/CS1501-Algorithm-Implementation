Given a list of possible orders, a cost limit, and a time limit, this project implements dynamic programming to build a table that produces the maximum number of cookies at each (costLimit, timeLimit) cell
Thus, it determines the maximum number of cookies that can be made under the constraints.
